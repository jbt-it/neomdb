# Environment variables for production build
# Environment variables set by the docker-compose file are cannot be accessed by the client
# Therefore, we need to set them here

# Environment variables for axios to connect to the backend in production
REACT_APP_API_URL=https://neomdb.studentische-beratung.de/api/
